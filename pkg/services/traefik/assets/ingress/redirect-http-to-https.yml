http:
  routers:
    http-to-https:
      entryPoints:
        - {http_entrypoint}
      rule: HostRegexp(`^.+$`)
      middlewares:
        - redirect-to-https
      service: noop@internal

  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true
